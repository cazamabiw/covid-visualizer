export interface CovidCase {
  label: string;
  value: number;
}

export interface CountryData {
  label: string;
  value: number;
}
export interface VaccinationData {
  label: string;
  value: number;
}